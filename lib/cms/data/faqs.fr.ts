// FR translation pending (D-018 Phase 8). Re-exports EN so the FR routes
// stay functional (fallback) until proper French copy is written.
export { generalFaqs, tourFaqs, faqById } from './faqs.en'
